# نظام مخازن بيشو - Bero Warehouse System v2.0

## 🚀 نظام إدارة مخازن شامل مبني بتقنية Tauri

### ✨ المزايا الجديدة في النسخة 2.0
- ⚡ **أداء فائق** - استهلاك ذاكرة أقل بـ 5-10 مرات من Electron
- 📦 **حجم صغير** - حجم التطبيق 3-10 ميجابايت فقط
- 🛡️ **أمان محسّن** - بنية أمنية قوية
- 🎯 **تطبيق native حقيقي** - بدون أي أثر للمتصفح
- 🚀 **سرعة تشغيل** - يفتح في أقل من ثانية

---

## 📋 المتطلبات الأساسية

قبل البدء، تأكد من تثبيت:

### 1. Node.js (الإصدار 16 أو أحدث)
- تحميل من: https://nodejs.org/

### 2. Rust (مطلوب لـ Tauri)
#### على Windows:
قم بتحميل وتثبيت `rustup` من: https://rustup.rs/

أو باستخدام PowerShell:
```powershell
winget install Rustlang.Rustup
```

بعد التثبيت، أعد تشغيل Terminal وتأكد من التثبيت:
```bash
rustc --version
cargo --version
```

### 3. Microsoft Visual Studio C++ Build Tools (لـ Windows)
- تحميل من: https://visualstudio.microsoft.com/visual-cpp-build-tools/
- أو استخدم: `winget install Microsoft.VisualStudio.2022.BuildTools`

---

## 🔧 التثبيت والإعداد

### الخطوة 1: تثبيت المكتبات
```bash
npm install
```

### الخطوة 2: تثبيت Tauri CLI (إذا لم يتم تثبيته تلقائياً)
```bash
npm install -D @tauri-apps/cli
```

---

## 🎮 التشغيل

### وضع التطوير (Development)
```bash
npm run tauri:dev
```
هذا الأمر سيقوم بـ:
- تشغيل React Dev Server على `http://localhost:3000`
- فتح نافذة Tauri التطبيق تلقائياً
- تفعيل Hot Reload للتطوير السريع

### بناء التطبيق للإنتاج (Production Build)
```bash
npm run tauri:build
```

التطبيق المبني سيكون في المجلد:
```
src-tauri/target/release/
```

ملف التثبيت للـ Windows سيكون في:
```
src-tauri/target/release/bundle/nsis/
```

---

## 📁 هيكل المشروع

```
bero-warehouse-system/
├── src/                      # كود React الرئيسي
│   ├── components/          # المكونات
│   ├── pages/               # الصفحات
│   ├── context/             # Context API
│   ├── services/            # الخدمات
│   └── utils/               # أدوات مساعدة
│
├── src-tauri/               # Tauri Backend (Rust)
│   ├── src/
│   │   └── main.rs         # نقطة الدخول الرئيسية
│   ├── Cargo.toml          # إعدادات Rust
│   ├── tauri.conf.json     # إعدادات Tauri
│   └── build.rs            # Build script
│
├── public/                  # الملفات العامة
├── package.json            # إعدادات npm
└── README.md               # هذا الملف
```

---

## 🎨 إضافة أيقونة مخصصة (اختياري)

### الطريقة التلقائية (موصى بها):
1. ضع ملف أيقونة PNG بحجم 1024x1024 في المجلد الرئيسي بإسم `app-icon.png`
2. قم بتثبيت أداة توليد الأيقونات:
```bash
npm install -D @tauri-apps/tauricon
```
3. قم بتوليد جميع أحجام الأيقونات:
```bash
npx @tauri-apps/tauricon app-icon.png
```

هذا سينشئ جميع الأيقونات المطلوبة في `src-tauri/icons/`

---

## 🛠️ الأوامر المتاحة

| الأمر | الوصف |
|-------|--------|
| `npm start` | تشغيل React فقط (للمتصفح) |
| `npm run build` | بناء React للإنتاج |
| `npm run tauri:dev` | تشغيل التطبيق في وضع التطوير |
| `npm run tauri:build` | بناء التطبيق للإنتاج |
| `npm test` | تشغيل الاختبارات |

---

## 🔐 الصلاحيات والأمان

التطبيق لديه الصلاحيات التالية (محددة في `tauri.conf.json`):
- ✅ قراءة وكتابة الملفات في مجلد `AppData`
- ✅ فتح نوافذ الحوار (فتح/حفظ/رسائل)
- ✅ الوصول لمعلومات النظام
- ✅ التحكم في النافذة (تكبير/تصغير/إغلاق)
- ❌ لا يوجد وصول للشبكة بدون إذن
- ❌ لا يوجد وصول لملفات النظام خارج AppData

---

## 📊 تخزين البيانات

حالياً، التطبيق يستخدم `localStorage` لتخزين البيانات.

**ملاحظة:** في المراحل القادمة، سيتم التحويل إلى قاعدة بيانات SQLite للحصول على:
- أداء أفضل
- سعة تخزين غير محدودة
- استعلامات SQL قوية
- نسخ احتياطي سهل

---

## 🐛 حل المشاكل الشائعة

### المشكلة: "Rust not found"
**الحل:** 
- تأكد من تثبيت Rust من https://rustup.rs/
- أعد تشغيل Terminal أو الكمبيوتر

### المشكلة: "MSVC build tools not found"
**الحل:**
- ثبّت Visual Studio Build Tools
- أو ثبّت Visual Studio Community مع C++ workload

### المشكلة: التطبيق لا يفتح
**الحل:**
- تأكد من عدم وجود برنامج آخر يستخدم المنفذ 3000
- جرب إغلاق وإعادة فتح Terminal

### المشكلة: التطبيق بطيء في أول مرة
**الحل:**
- المرة الأولى تستغرق وقتاً أطول بسبب تجميع Rust
- المرات القادمة ستكون أسرع بكثير

---

## 🚀 التوزيع

بعد بناء التطبيق بـ `npm run tauri:build`، ستجد:

### على Windows:
- ملف `.exe` قابل للتشغيل مباشرة
- ملف `.msi` للتثبيت (NSIS installer)

المسار:
```
src-tauri/target/release/bundle/nsis/bero-warehouse-system_2.0.0_x64-setup.exe
```

---

## 📝 ملاحظات مهمة

1. **البيانات محفوظة** - جميع بياناتك من localStorage ستعمل كما هي
2. **نفس الواجهة** - لم يتغير أي شيء في الواجهة أو الوظائف
3. **أداء أفضل** - ستلاحظ تحسن كبير في الأداء والسرعة
4. **حجم أصغر** - التطبيق أصغر حجماً بكثير من Electron

---

## 📞 الدعم

للمساعدة أو الإبلاغ عن مشاكل، يرجى التواصل مع فريق التطوير.

---

## 📄 الترخيص

هذا المشروع ملك لشركة Bero System - جميع الحقوق محفوظة © 2025

---

**تم التطوير بواسطة فريق Bero System** 🚀
